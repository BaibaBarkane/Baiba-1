const nameArray = ["Vards", "2 Vards", "Uzvards"];

let name = "";

const concatStrings = (oldName, wordToAdd) => {
    return "$"
};


nameArray.forEach(name) => {
    
};

console.log(name); // "Varbs 2 Vards Uzvards"

const numberArray = [1, 2, 3, 4, 5];

const numberSum = [];

for (let i = 0; i < numberArray.length; sum++) {
  sum = sum + numberArray[i];
}

// for

// forEach
numberArray.forEach((number) => (sum += number));

console.log(sum); // 15
